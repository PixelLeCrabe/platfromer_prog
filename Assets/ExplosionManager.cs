using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExplosionManager : MonoBehaviour
{
    public Grab grab;
    public string HitEntity;
        
    void Start()
    {

    }
    
        // object THROWN 1,2,3         ExplosionManager          mob1,2,3
        //  ki il hit                                           verifier si il est hit par un obj

    

    // Enemy is hit by a thrown object 
    void EnemyIsHitByExplosion()
    {
        //if(HitEntity == C.gameObject.name)   { }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
